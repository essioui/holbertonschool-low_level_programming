#include <stdio.h>
/**
 * main - changes word upper to lowercase
 *
 * Return: Always (Success)
 */
int main(void)
{
char ch;
for (ch = 'a'; ch <= 'z'; ch++)
{
putchar(ch);
}
putchar ('\n');
return (0);
}

